name = "mania-editor"
main = "src/index.ts"
compatibility_date = "2024-11-27"

# Plaintext runtime vars live in [vars] below so `wrangler deploy` (including
# Workers Builds) keeps them; dashboard-only plaintext vars would be wiped on
# every deploy. Encrypted *secrets* stay in the dashboard and survive deploys:
#   CLIENT_SECRET              osu! OAuth client secret
#   SUPABASE_SERVICE_ROLE_KEY  Supabase service-role key (server only)
#   SUPABASE_JWT_SECRET        Supabase JWT secret (HS256) used to mint user tokens
#   COOKIE_SECRET              random string, signs the session cookie

[vars]
CLIENT_ID = "60987"
FRONTEND_URL = "https://cascade.sheepex.net"
OSU_REDIRECT_URI = "https://mania-editor.noahcraft01.workers.dev/auth/osu/callback"
SUPABASE_URL = "https://splqsxhwdusjeqthinxz.supabase.co"

{
  "name": "mania-editor-worker",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Cloudflare Worker: osu! OAuth + Supabase session minting for the mania editor",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types --include-runtime false",
    "typecheck": "wrangler types --include-runtime false --check && tsc --noEmit"
  },
  "dependencies": {
    "jose": "^5.9.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260813.1",
    "@types/node": "^26.2.0",
    "typescript": "^5.6.3",
    "wrangler": "^4.122.0"
  }
}

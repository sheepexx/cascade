/**
 * The interface font, for text drawn where CSS does not reach: canvases and
 * exported images. Keep it in step with the `sans` family in
 * tailwind.config.js, or canvas labels fall back to a different face.
 */
export const FONT_STACK =
  '"Quicksand", "Inter", ui-sans-serif, system-ui, -apple-system, "Segoe UI", Roboto, "PingFang SC", "Hiragino Sans GB", "Microsoft YaHei", "Noto Sans CJK SC", "Source Han Sans SC", sans-serif';

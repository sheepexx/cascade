# Notes for Claude

- Commit as `sheepex_ <148464281+sheepexx@users.noreply.github.com>`. Set `git config user.name` and `git config user.email` to that before the first commit of a session.
- Never credit or mention Claude in commits: no `Co-Authored-By` trailer, no `Claude-Session` link, no "Generated with Claude Code" line, and no mention of Claude or of this file anywhere in a commit message.
- Commit and push straight to `master`. Don't open pull requests or work on other branches.

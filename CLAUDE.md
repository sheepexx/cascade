# Notes for Claude

- Commit as `sheepex_ <148464281+sheepexx@users.noreply.github.com>`. Set `git config user.name` and `git config user.email` to that before the first commit of a session.
- Never credit Claude anywhere: no `Co-Authored-By` trailer, no `Claude-Session` link and no "Generated with Claude Code" line in commits, pull requests or anything else.
- Don't open pull requests.
